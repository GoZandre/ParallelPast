using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MouseHidder : MonoBehaviour
{
    /*
    void Update()
    {
        float inputController = Input.GetAxisRaw("Vertical") + Input.GetAxisRaw("Horizontal");
        float inputMouse = Input.GetAxis("Mouse X") + Input.GetAxis("Mouse Y");
        if (inputController != 0)
        {
            Cursor.visible = false;
            Cursor.lockState = CursorLockMode.Locked;
        }
        else if (inputMouse != 0)
        {
            Cursor.visible = true;
            Cursor.lockState = CursorLockMode.None;
            
        }
}*/
}
